# GoVoice mic (USB2.0 Device, VID:PID 8087:1024) — dat gain 35% moi khi cam vao.
# Cai: sudo cp 99-govoice-mic.rules /etc/udev/rules.d/  &&  sudo udevadm control --reload
ACTION=="add", SUBSYSTEM=="usb", ATTR{idVendor}=="8087", ATTR{idProduct}=="1024", RUN+="/usr/local/bin/govoice-mic-setup.sh"
